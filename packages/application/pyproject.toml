[project]
name = "axion-application"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = ["axion-domain", "axion-contracts", "pydantic==2.13.4"]

[build-system]
requires = ["setuptools>=80"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
